
function Component(){
    return(
        <>
   <h3>This is just a component  </h3>
   <p>In this we are creating a custom component in and these can be exported to index.js file so that it can render it directly by using reactdom</p>
        </>
    );
}

export default Component;
